{
  "name": "ami-scripts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Operational scripts: embedding backfill + Phase 3 ingestion pipeline.",
  "engines": {
    "node": ">=20.9.0"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "backfill": "tsx backfill-embeddings.ts",
    "ingest": "tsx ingest/run.ts"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.107.0"
  },
  "devDependencies": {
    "@types/node": "^20",
    "tsx": "^4.19.2",
    "typescript": "^5"
  }
}
